<nav>
    <ul>
      <li><a href="/private">Profile</a></li>
      <li><a href="/auth/logout">Logout</a></li>
    </ul>
</nav>
<h1>HOME PAGE</h1>
<a class="btn btn-primary" href="auth/signup">SIGN UP</a>
<h2>the posts</h2>


<a class="btn btn-primary" href="/createpost">NEW POST</a>

  {{#each posts as |post|}}
  <div class="card" style="width: 18rem;">
    <img src="{{post.picPath}}" class="card-img-top" alt="...">
    <div class="card-body">
  <p class="card-text">
  {{post.content}} </p>
</div>
    </div>
  {{/each}}

 

  <a class="btn btn-primary" href=" /createcomment">NEW COMMENT</a>
  <h2>Comments</h2>
  <div class="post-comments">
  {{#each comments as |comment|}}
    <div class="comment-item">
      <img src="{{comment.comImgPath}}" alt="{{comment.comImgName}}" id="photocom">
      <p>{{comment.content}}</p>
    </div>
  {{/each}}
 

</div>
